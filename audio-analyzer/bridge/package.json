{
  "name": "rta-bridge",
  "version": "1.0.0",
  "private": true,
  "description": "RTA Bridge — LAN sidecar that speaks real audio-console / audio-network protocols (OSC/UDP, mDNS) and exposes one versioned WebSocket JSON API to the RTA Insight Pro web app.",
  "license": "UNLICENSED",
  "type": "commonjs",
  "main": "dist/index.js",
  "scripts": {
    "lint": "tsc --noEmit -p tsconfig.test.json",
    "test": "vitest run",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "@types/ws": "^8.5.10",
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
